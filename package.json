{
  "name": "nexus-ai-studio",
  "private": true,
  "packageManager": "pnpm@9.12.3",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "db:push": "turbo run db:push --filter=@nexus/db",
    "db:studio": "turbo run db:studio --filter=@nexus/db",
    "vercel-build": "turbo build --filter=@nexus/web..."
  },
  "devDependencies": {
    "turbo": "^2.2.3",
    "typescript": "^5.6.3",
    "@types/node": "22.11.0",
    "prettier": "^3.3.3"
  },
  "engines": {
    "node": "22.x"
  },
  "packageManager": "pnpm@9.12.3",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
